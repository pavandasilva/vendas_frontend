import React from 'react'

import { Container } from './styles'

interface TabsProps {
  fixedContent?: React.ReactNode,
  titleFixedContent: string
}

const Tabs = ({ fixedContent, titleFixedContent }: TabsProps) => {
  return (
    <Container>

    </Container>
  )
}

export default Tabs
